syntax = "proto3";
package naki3d.common.protocol;

enum HandGestureType {
    // Shared
    GESTURE_SWIPE_LEFT = 0;
    GESTURE_SWIPE_RIGHT = 1;
    GESTURE_SWIPE_UP = 2;
    GESTURE_SWIPE_DOWN = 3;

    // Mediapipe specific
    GESTURE_CLOSE_HAND = 4;
    GESTURE_OPEN_HAND = 5;
    GESTURE_PINCH = 6;
}

enum HandSide {
    LEFT = 0;
    RIGHT = 1;
}